{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${description}",
  "environment": "*",
  "entrypoints": {
    "client": [
      "com.argon.client.ArgonClient"
    ]
  },
  "mixins": [
    "argon.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": ">=${minecraft_version}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "sources": "https://github.com/yourname/argon-client"
      }
    }
  }
}